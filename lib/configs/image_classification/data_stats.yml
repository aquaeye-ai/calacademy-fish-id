#######################
# CONFIG for pcr_data_stats.py #
#######################

#NOTE:
# 1) bool is treated as int, > 0 for true or <= 0 for false,
# 2) paths are relative to top level directory: calacademy-fish-id/

# data directory
directory: /home/nightrider/calacademy-fish-id/datasets/image_classification/reef_lagoon/scraped_web/groupings/training_splits/8_12_2020/1

# dataset type so the script knows how to parse the data
db_type: training_splits

# only necessary if generating stats for db_type of training_splits, e.g. train/test/val
splits:
  - train
  - test